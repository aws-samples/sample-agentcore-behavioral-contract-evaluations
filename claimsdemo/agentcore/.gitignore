# Secrets (local environment files are never committed)
.env.local

# CDK Build Artifacts
cdk/cdk.out/
cdk/node_modules/

# CLI Internals (deployment state is environment-specific, never committed)
.cli/*

# Ephemeral Staging
.cache/*

# Deployment targets contain account IDs - copy aws-targets.json.example
aws-targets.json
